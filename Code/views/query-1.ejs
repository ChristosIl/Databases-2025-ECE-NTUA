<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Query 1 Results</title>
  <style>

    body {
            margin: 0;
            padding: 0;
            font-family: Arial, sans-serif;
            text-align: center;
            background-color: rgb(255, 255, 255);
        }

    table, th, td {
      border: 1px solid black;
      border-collapse: collapse;
      padding: 10px;
      margin: 0 auto;
      border-collapse: collapse ;
      font-family:Arial, Helvetica, sans-serif;
      
    }

    
    h1 {
          margin-bottom: 20px;
          color: white;
          font-size: 40px;
          font-family: 'Courier New', Courier, monospace;
          width: 100%;
    }

    .title-section {
            background-color: blue; /* Ροζ ανοιχτό */
            padding: 40px;
            border-radius: 0px;
            margin-bottom: 30px;
        }
    
        .Back-Home a{
          font-family: 'Courier New', Courier, monospace;
          font-size: 25px;
          color: black;
        }

        .Back-Home a:hover {
          color: blue;
          text-decoration: underline;
        }
  </style>
</head>
<body>
  <div class="title-section">
    <h1>Query 1 Results</h1>
  </div>
  
  <div style="margin: 20px; padding: 15px; background-color: #f5f5f5; border: 1px solid #ddd; border-radius: 5px; text-align: left; overflow: auto; max-width: 90%; margin-left: auto; margin-right: auto;">
    <h3>SQL Query:</h3>
    <pre style="white-space: pre-wrap; font-family: 'Courier New', monospace;"><%= queryString %></pre>
  </div>
  
  <table>
    <thead>
      <tr>
        <th>Festival</th>
        <th>Year</th>
        <th>Payment Method</th>
        <th>Ticket Type</th>
        <th>Total Revenue (€)</th>
      </tr>
    </thead>
    <tbody>
      <% query1.forEach(row => { %>
        <tr>
          <td><%= row.festival_name %></td>
          <td><%= row.festival_year %></td>
          <td><%= row.Payment_method %></td>
          <td><%= row.Ticket_type %></td>
          <td><%= Number(row.Total_revenue).toFixed(2) %></td>
        </tr>
      <% }); %>
    </tbody>
  </table>
  <br>

  <div class="Back-Home">
    <a href="/index">Back to Home</a>
  </div>
</body>
</html>